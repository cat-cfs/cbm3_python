import os

INSTALL_PATH = os.path.join(
    "c:\\", "Program Files (x86)", "Operational-Scale CBM-CFS3")

ARCHIVE_INDEX_PATH = os.path.join(
    INSTALL_PATH, "Admin", "DBs", "ArchiveIndex_Beta_Install.mdb")

CBM_EXECUTABLE_DIR = os.path.join(
    INSTALL_PATH, "Admin", "Executables")
